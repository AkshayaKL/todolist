

export default class task{ 
     constructor(date, author, status, name,id){
         this.date = date;
         this.author = author;
         this.status= status;
         this.name = name;
         this.id=id;
     }
}